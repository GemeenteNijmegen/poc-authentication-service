// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for JwksFunction
 */
export interface JwksFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/well-known/jwks/jwks.
 */
export class JwksFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: JwksFunctionProps) {
    super(scope, id, {
      description: 'src/well-known/jwks/jwks.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs20.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/well-known/jwks/jwks.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}